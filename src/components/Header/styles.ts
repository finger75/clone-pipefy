import styled from 'styled-components';

export const Container = styled.div`
    height: 80px;
    padding: 0 0 0 30px;
    background: #7159c1;
    color: var(--white);

    display: flex;
    align-items: center;
`;

export default Container;